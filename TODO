mac
	Add touch_id sudo enabler.
	Install iTerm2 and a bunch of other programs
	Install fish shell and set as default
	Add Homebrew scripts. (See win/scripts/wsl.ps1)
	Add generic init scripts.

win
	Wrap all WSL commands into a bash script
